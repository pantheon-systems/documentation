# https://circleci.com/docs/configuration#machine
machine:
  php:
    # https://circleci.com/docs/environment#php
    version: 5.5.11

dependencies:
  pre:
    # Run composer
    - composer install
test:
    #
    # This is where the documentation will be built.
    # or otherwise. Here's a SimpleTest example.
    - ./vendor/sculpin/sculpin/bin/sculpin generate
  override:
  # Perform post build operations
  post:
# Custom notifications
notify:
  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
